# Data Cleaning Framework

Clean in this order: back up raw data, define clean schema, standardize column names, fix data types, trim and normalize text, standardize categories, handle missing values, deduplicate, validate totals/counts, and document changes. Check row counts, totals, unique IDs, missing rates, and category counts before and after cleaning.
