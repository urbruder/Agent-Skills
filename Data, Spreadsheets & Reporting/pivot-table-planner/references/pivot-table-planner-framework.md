# Pivot Planning Framework

Rows define what to compare down the side. Columns define a secondary split. Values define the metric. Filters narrow the dataset. Use sum for revenue and quantities, count for records, distinct count for customers/orders, and calculated fields for margin, conversion, AOV, and rates. Group dates by week, month, quarter, or year depending on reporting cadence.
